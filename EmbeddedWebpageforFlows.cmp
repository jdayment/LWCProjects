<aura:component implements="lightning:availableForFlowScreens,flexipage:availableForAllPageTypes,forceCommunity:availableForAllPageTypes" access="global">
    <aura:attribute name="iframeUrl" type="String" />
    <aura:attribute name="width" type="String" />
    <aura:attribute name="height" type="String" />
    <aura:attribute name="scrolling" type="String" />
    <aura:attribute name="frameBorder" type="String" />
    <aura:attribute name="style" type="String" />
    <aura:attribute name="sandbox" type="String" />
    <aura:attribute name="allow" type="String" />
    <aura:attribute name="allowfullscreen" type="Boolean" />
    
    
        
    <iframe src="{!v.iframeUrl}"
            width="{!v.width}"
            height="{!v.height}"
            style="{!v.style}"
            sandbox="{!v.sandbox}"
            frameBorder="{!v.frameBorder}"
            scrolling="{!v.scrolling}"
            allow="{!v.allow}"
                        />
    
</aura:component>